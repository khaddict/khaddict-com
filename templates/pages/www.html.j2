{% from "partials/js_str.j2" import js_str %}
<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script>
{% include "partials/theme_toggle.js.j2" %}
  </script>
  <title>{{ i18n['title.home'] }}</title>
  <meta name="description" content="{{ meta_description }}" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="{{ og_url }}" />
  <meta property="og:title" content="khaddict" />
  <meta property="og:description" content="{{ meta_description }}" />
  <meta property="og:image" content="{{ brand_icon_src }}" />
  <meta property="og:locale" content="{{ og_locale }}" />
  <meta property="og:locale:alternate" content="{{ og_locale_alternate }}" />
  <link rel="canonical" href="{{ canonical_url }}" />
  <link rel="alternate" hreflang="en" href="{{ hreflang_en_href }}" />
  <link rel="alternate" hreflang="fr" href="{{ hreflang_fr_href }}" />
  <link rel="alternate" hreflang="x-default" href="{{ hreflang_x_default_href }}" />
  <meta name="twitter:card" content="summary" />
  <meta name="twitter:title" content="khaddict" />
  <meta name="twitter:image" content="{{ brand_icon_src }}" />
  <link rel="icon" href="{{ brand_icon_src }}" type="image/png" />
  <link rel="apple-touch-icon" href="{{ brand_icon_src }}" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700;800&display=swap" rel="stylesheet" />

  <!-- Matomo -->
  <script>
    var _paq = window._paq = window._paq || [];
    _paq.push(['trackPageView']);
    _paq.push(['enableLinkTracking']);
    (function() {
      var u="//matomo.khaddict.com/";
      _paq.push(['setTrackerUrl', u+'matomo.php']);
      _paq.push(['setSiteId', '1']);
      var d=document, g=d.createElement('script'), s=d.getElementsByTagName('script')[0];
      g.async=true; g.src=u+'matomo.js'; s.parentNode.insertBefore(g,s);
    })();
  </script>
  <!-- End Matomo -->

  <style>
{% include "partials/head_tokens.css.j2" %}
{% include "partials/status_widget.css.j2" %}
    /* hero */
    .hero {
      flex: 1;
      display: flex;
      align-items: flex-start;
      justify-content: center;
      padding: 1.25rem 1.5rem;
      animation: fade-up .45s ease both;
    }

    .hero-row {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 3rem;
      max-width: 1400px;
      width: 100%;
    }

    .hero-copy { flex: 0 1 500px; text-align: left; }

    .hero h1 {
      font-size: clamp(1.5rem, 4vw, 2.1rem);
      font-weight: 700;
      line-height: 1.4;
      max-width: 500px;
      letter-spacing: -.02em;
    }

    @media (max-width: 940px) {
      .hero-row { flex-direction: column; text-align: center; gap: 2rem; }
      .hero-copy { flex: none; text-align: center; max-width: 100%; }
      .hero h1 { max-width: 100%; margin: 0 auto; }
    }

    .hero .highlight {
      background: linear-gradient(90deg, var(--accent), var(--accent-2));
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

    .repo-link {
      display: inline-flex;
      align-items: center;
      gap: .4rem;
      margin-top: .9rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: .8rem;
      color: var(--muted);
      text-decoration: none;
      transition: color .15s ease;
    }
    .repo-link:hover { color: var(--accent); }
    .repo-link svg { width: 14px; height: 14px; }

    @keyframes fade-up {
      from { opacity: 0; transform: translateY(10px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* busy bar wall */
    .wall-card {
      position: relative;
      display: flex;
      flex-direction: column;
      gap: .6rem;
      padding: 1.1rem 1.5rem;
      border-radius: 16px;
      background: var(--surface);
      border: 1px solid var(--border);
      box-shadow: 0 20px 50px var(--shadow);
      flex: 1 1 520px;
      max-width: 650px;
      width: 100%;
      transition: background-color .2s ease, border-color .2s ease;
    }

    .wall-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
    }

    .wall-eyebrow {
      font-size: .68rem;
      font-weight: 600;
      letter-spacing: .1em;
      text-transform: uppercase;
      color: var(--accent);
    }

    .wall-live-pill {
      display: inline-flex;
      align-items: center;
      gap: .4rem;
      font-size: .7rem;
      font-weight: 600;
      color: var(--text);
      background: var(--up-bg);
      transition: background .3s ease;
      padding: .28rem .6rem .28rem .5rem;
      border-radius: 999px;
      flex-shrink: 0;
      white-space: nowrap;
    }

    .wall-live-pill.down { background: var(--down-bg); }

    .wall-card h2 {
      font-size: 1.05rem;
      font-weight: 700;
      letter-spacing: -.01em;
      margin: -.4rem 0 0;
    }

    .wall-scene {
      position: relative;
      width: 100%;
      aspect-ratio: 1672 / 941;
      border-radius: 8px;
      overflow: hidden;
      background-image: url('/wall-scene.png');
      background-size: cover;
      background-position: center;
    }

    .wall-scene canvas {
      position: absolute;
      left: 25.60%;
      top: 62.38%;
      width: 12.74%;
      height: 4.78%;
      display: block;
      image-rendering: pixelated;
    }

    .wall-port-led {
      position: absolute;
      width: 1.6%;
      height: .75%;
      border-radius: 50%;
      background: radial-gradient(ellipse at center, rgba(70, 255, 140, 1) 0%, rgba(70, 255, 140, .55) 45%, rgba(70, 255, 140, 0) 80%);
      transform: translate(-50%, -50%);
      mix-blend-mode: screen;
      animation: port-led-flicker 2.4s ease-in-out infinite;
      pointer-events: none;
    }

    .wall-port-led.amber {
      background: radial-gradient(ellipse at center, rgba(255, 190, 60, 1) 0%, rgba(255, 190, 60, .55) 45%, rgba(255, 190, 60, 0) 80%);
    }

    @keyframes port-led-flicker {
      0%, 100% { opacity: .8; }
      4% { opacity: .25; }
      7% { opacity: .9; }
      10% { opacity: .3; }
      14% { opacity: 1; }
      45% { opacity: .8; }
      60% { opacity: .3; }
      64% { opacity: 1; }
      90% { opacity: .8; }
    }

    .wall-crystal-aura {
      position: absolute;
      left: 93.3%;
      top: 8.5%;
      width: 7%;
      height: 14%;
      transform: translate(-50%, -50%);
      border-radius: 50%;
      background: radial-gradient(circle, rgba(140, 110, 255, .55) 0%, rgba(110, 80, 255, .25) 40%, rgba(110, 80, 255, 0) 70%);
      animation: crystal-aura-pulse 4s ease-in-out infinite;
      pointer-events: none;
      mix-blend-mode: screen;
    }

    .wall-keyboard-glow {
      position: absolute;
      left: 48.6%;
      top: 93.62%;
      width: 34%;
      height: 1.2%;
      transform: translate(-50%, -50%);
      border-radius: 50%;
      background: radial-gradient(ellipse at center, rgba(160, 60, 255, .75) 0%, rgba(140, 50, 255, .35) 40%, rgba(140, 50, 255, 0) 65%);
      animation: crystal-aura-pulse 3.6s ease-in-out infinite;
      pointer-events: none;
      mix-blend-mode: screen;
    }

    @keyframes crystal-aura-pulse {
      0%, 100% { opacity: .55; transform: translate(-50%, -50%) scale(.9); }
      50% { opacity: 1; transform: translate(-50%, -50%) scale(1.15); }
    }

    .wall-toolbar {
      display: flex;
      align-items: center;
      align-self: flex-start;
      gap: .6rem;
    }

    .wall-mode-toggle {
      display: inline-flex;
      border: 1px solid var(--border);
      border-radius: 999px;
      padding: 3px;
      gap: 2px;
    }

    .wall-mode-toggle button {
      font-family: 'JetBrains Mono', monospace;
      font-size: .72rem;
      font-weight: 600;
      color: var(--muted);
      background: none;
      border: none;
      border-radius: 999px;
      padding: .35rem .8rem;
      cursor: pointer;
      transition: background .15s ease, color .15s ease;
    }

    .wall-mode-toggle button.active {
      color: #fff;
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
    }

    .wall-color-picker {
      position: relative;
    }

    .wall-color-pill {
      display: inline-flex;
      align-items: center;
      font-family: 'JetBrains Mono', monospace;
      font-size: .7rem;
      font-weight: 600;
      color: var(--text);
      background: var(--border);
      border: none;
      border-radius: 999px;
      padding: .28rem .7rem;
      cursor: pointer;
      transition: background .15s ease;
    }

    .wall-color-popover {
      display: none;
      position: absolute;
      top: calc(100% + .5rem);
      left: 0;
      align-items: center;
      gap: .4rem;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 999px;
      padding: .35rem;
      box-shadow: 0 8px 20px var(--shadow);
      z-index: 5;
    }

    .wall-color-popover.open { display: flex; }

    .wall-color-swatch {
      width: 1rem;
      height: 1rem;
      border-radius: 50%;
      border: none;
      padding: 0;
      cursor: pointer;
      box-shadow: inset 0 0 0 1px rgba(0, 0, 0, .15);
      transition: transform .12s ease;
    }

    .wall-color-swatch:hover { transform: scale(1.15); }

    .wall-color-swatch.active {
      outline: 2px solid var(--text);
      outline-offset: 2px;
    }

    #wall-image-field { height: 3rem; }

    .wall-drop-zone {
      height: 100%;
      box-sizing: border-box;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 1px dashed var(--border);
      border-radius: 8px;
      padding: .4rem 1rem;
      text-align: center;
      cursor: pointer;
      transition: border-color .15s ease, background .15s ease;
    }

    .wall-drop-zone:hover, .wall-drop-zone:focus-visible, .wall-drop-zone.dragover {
      border-color: var(--accent);
      background: rgba(139, 92, 246, .05);
      outline: none;
    }

    .wall-drop-zone p {
      margin: 0;
      font-size: .76rem;
      color: var(--muted);
    }

    .wall-drop-zone strong { color: var(--text); }

    .wall-text-row { position: relative; height: 3rem; }

    .wall-text-row input {
      width: 100%;
      height: 100%;
      box-sizing: border-box;
      font-family: 'JetBrains Mono', monospace;
      font-variant-ligatures: none;
      -webkit-font-feature-settings: "liga" 0, "calt" 0;
      font-feature-settings: "liga" 0, "calt" 0;
      font-size: .85rem;
      color: var(--text);
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 0 3.4rem 0 .7rem;
    }

    .wall-text-row input:focus { outline: none; border-color: var(--accent); }

    .wall-counter {
      position: absolute;
      right: .7rem;
      top: 50%;
      transform: translateY(-50%);
      font-size: .64rem;
      color: var(--muted);
      font-variant-numeric: tabular-nums;
    }

    .wall-counter.warn { color: var(--accent-2); }

    .wall-actions {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      flex-wrap: wrap;
      margin-top: .5rem;
    }

    .wall-send-group {
      display: flex;
      align-items: center;
      gap: .75rem;
    }

    .wall-learn-more {
      font-family: 'JetBrains Mono', monospace;
      font-size: .76rem;
      color: var(--muted);
      text-decoration: none;
      transition: color .15s ease;
    }

    .wall-learn-more:hover { color: var(--accent); }

    .wall-send {
      font-family: 'JetBrains Mono', monospace;
      font-size: .8rem;
      font-weight: 700;
      color: #fff;
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
      border: none;
      border-radius: 999px;
      padding: .55rem 1.3rem;
      cursor: pointer;
      transition: transform .12s ease, opacity .15s ease;
    }

    .wall-send:hover:not(:disabled) { transform: translateY(-1px); }
    .wall-send:disabled { opacity: .55; cursor: not-allowed; transform: none; }

    .wall-toast {
      display: none;
      font-size: .74rem;
      color: var(--up);
      opacity: 0;
      transform: translateY(4px);
      transition: opacity .18s ease, transform .18s ease;
    }

    .wall-toast.show { display: inline-block; opacity: 1; transform: translateY(0); }
    .wall-toast.error { color: var(--down); }

    @media (max-width: 940px) {
      .wall-card { flex: none; }
      .wall-head { flex-direction: column; gap: .5rem; }
      .wall-actions { justify-content: center; }
    }
{% include "partials/responsive.css.j2" %}
  </style>
</head>

<body>
{% include "partials/header.html.j2" %}

  <main class="hero">
    <div class="hero-row">
      <div class="hero-copy">
        <h1 data-i18n="hero.title">{{ i18n['hero.title'] | safe }}</h1>
        <a class="repo-link" href="https://github.com/khaddict/voidnode" target="_blank" rel="noopener">
          <svg viewBox="0 0 24 24" fill="currentColor" stroke="none"><path d="M12 2C6.48 2 2 6.58 2 12.25c0 4.53 2.87 8.37 6.84 9.73.5.1.68-.22.68-.49 0-.24-.01-1.03-.01-1.87-2.78.62-3.37-1.21-3.37-1.21-.46-1.19-1.11-1.51-1.11-1.51-.91-.64.07-.62.07-.62 1 .07 1.53 1.05 1.53 1.05.89 1.56 2.34 1.11 2.91.85.09-.66.35-1.11.63-1.37-2.22-.26-4.56-1.14-4.56-5.06 0-1.12.39-2.03 1.03-2.75-.1-.26-.45-1.31.1-2.72 0 0 .84-.28 2.75 1.05a9.3 9.3 0 0 1 5 0c1.91-1.33 2.75-1.05 2.75-1.05.55 1.41.2 2.46.1 2.72.64.72 1.03 1.63 1.03 2.75 0 3.93-2.34 4.79-4.57 5.05.36.32.68.94.68 1.9 0 1.37-.01 2.47-.01 2.81 0 .27.18.6.69.49A10.03 10.03 0 0 0 22 12.25C22 6.58 17.52 2 12 2z"/></svg>
          github.com/khaddict/voidnode
        </a>
      </div>

      <div class="wall-card">
        <div class="wall-head">
          <span class="wall-eyebrow" data-i18n="wall.eyebrow">{{ i18n['wall.eyebrow'] }}</span>
          <span class="wall-live-pill" id="wall-live-pill"><span class="dot up" id="wall-live-dot"></span><span data-i18n="wall.live" id="wall-live-text">{{ i18n['wall.live'] }}</span></span>
        </div>
        <h2 data-i18n="wall.title">{{ i18n['wall.title'] }}</h2>

        <div class="wall-scene">
          <canvas id="wall-screen" width="288" height="64"></canvas>
          <span class="wall-port-led" style="left:17.05%;top:45.48%;animation-duration:2.2s;animation-delay:0s"></span>
          <span class="wall-port-led" style="left:18.24%;top:45.16%;animation-duration:2.6s;animation-delay:.9s"></span>
          <span class="wall-port-led amber" style="left:19.44%;top:44.85%;animation-duration:1.9s;animation-delay:.4s"></span>
          <span class="wall-port-led" style="left:20.87%;top:44.53%;animation-duration:2.4s;animation-delay:1.5s"></span>
          <span class="wall-crystal-aura"></span>
          <span class="wall-keyboard-glow"></span>
        </div>

        <div class="wall-toolbar">
          <div class="wall-mode-toggle">
            <button type="button" class="active" data-mode="text" data-i18n="wall.modeText">{{ i18n['wall.modeText'] }}</button>
            <button type="button" data-mode="image" data-i18n="wall.modeImage">{{ i18n['wall.modeImage'] }}</button>
          </div>

          <div class="wall-color-picker" id="wall-color-picker">
            <button type="button" class="wall-color-pill" id="wall-color-trigger" aria-haspopup="true" aria-expanded="false">
              <span data-i18n="wall.color.pick">{{ i18n['wall.color.pick'] }}</span>
            </button>
            <div class="wall-color-popover" id="wall-color-popover">
              <button type="button" class="wall-color-swatch active" data-color="#FFFFFF" style="background:#FFFFFF" aria-label="{{ i18n['wall.color.white'] }}" data-i18n-aria="wall.color.white"></button>
              <button type="button" class="wall-color-swatch" data-color="#FF0000" style="background:#FF0000" aria-label="{{ i18n['wall.color.red'] }}" data-i18n-aria="wall.color.red"></button>
              <button type="button" class="wall-color-swatch" data-color="#FF8800" style="background:#FF8800" aria-label="{{ i18n['wall.color.orange'] }}" data-i18n-aria="wall.color.orange"></button>
              <button type="button" class="wall-color-swatch" data-color="#FFFF00" style="background:#FFFF00" aria-label="{{ i18n['wall.color.yellow'] }}" data-i18n-aria="wall.color.yellow"></button>
              <button type="button" class="wall-color-swatch" data-color="#00FF00" style="background:#00FF00" aria-label="{{ i18n['wall.color.green'] }}" data-i18n-aria="wall.color.green"></button>
              <button type="button" class="wall-color-swatch" data-color="#00FFFF" style="background:#00FFFF" aria-label="{{ i18n['wall.color.cyan'] }}" data-i18n-aria="wall.color.cyan"></button>
              <button type="button" class="wall-color-swatch" data-color="#0066FF" style="background:#0066FF" aria-label="{{ i18n['wall.color.blue'] }}" data-i18n-aria="wall.color.blue"></button>
              <button type="button" class="wall-color-swatch" data-color="#FF00FF" style="background:#FF00FF" aria-label="{{ i18n['wall.color.magenta'] }}" data-i18n-aria="wall.color.magenta"></button>
            </div>
          </div>
        </div>

        <div class="wall-text-row" id="wall-text-field">
          <input type="text" id="wall-message" maxlength="60">
          <span class="wall-counter" id="wall-counter">0/60</span>
        </div>

        <div id="wall-image-field" style="display:none;">
          <div class="wall-drop-zone" id="wall-drop-zone" tabindex="0">
            <p data-i18n="wall.dropZone">{{ i18n['wall.dropZone'] | safe }}</p>
          </div>
          <input type="file" id="wall-file-input" accept="image/png,image/jpeg,image/webp" style="display:none;">
        </div>

        <div class="wall-actions">
          <a class="wall-learn-more" href="{{ nav_blog_href }}{{ 'posts/busy-bar-message-wall/' if lang == 'fr' else '/posts/busy-bar-message-wall/' }}" data-i18n="wall.learnMore">{{ i18n['wall.learnMore'] }}</a>
          <div class="wall-send-group">
            <div class="wall-toast" id="wall-toast" data-i18n="wall.sent">{{ i18n['wall.sent'] }}</div>
            <button class="wall-send" id="wall-send" type="button" data-i18n="wall.send">{{ i18n['wall.send'] }}</button>
          </div>
        </div>
      </div>
    </div>
  </main>

{% include "partials/footer.html.j2" %}

  <script>
    const I18N = {
{% for locale in ['en', 'fr'] %}
      {{ locale }}: {
{% for key, value in i18n_all[locale].items() %}
        '{{ key }}': {{ js_str(value) }},
{% endfor %}
      },
{% endfor %}
    };

    let currentLang = '{{ lang }}';

    function t(key) {
      return (I18N[currentLang] && I18N[currentLang][key]) || I18N.en[key] || key;
    }

    function applyLanguage() {
      document.documentElement.lang = currentLang;
      document.title = t('title.home');
      document.querySelectorAll('[data-i18n]').forEach((el) => {
        el.innerHTML = t(el.getAttribute('data-i18n'));
      });
      document.querySelectorAll('[data-i18n-aria]').forEach((el) => {
        el.setAttribute('aria-label', t(el.getAttribute('data-i18n-aria')));
      });
      document.getElementById('lang-current').textContent = currentLang.toUpperCase();
      document.querySelectorAll('.lang-option').forEach((el) => {
        el.classList.toggle('active', el.getAttribute('data-lang') === currentLang);
      });
    }

    applyLanguage();

    const langBtn = document.getElementById('lang-btn');
    const langDropdown = document.getElementById('lang-dropdown');

    langBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      const isOpen = langDropdown.classList.toggle('open');
      langBtn.setAttribute('aria-expanded', isOpen);
    });
    document.addEventListener('click', (e) => {
      if (!langDropdown.contains(e.target) && e.target !== langBtn) {
        langDropdown.classList.remove('open');
        langBtn.setAttribute('aria-expanded', 'false');
      }
    });
{% include "partials/status_widget.js.j2" %}

    document.getElementById('theme-toggle').addEventListener('click', () => {
      const next = document.documentElement.dataset.theme === 'dark' ? 'light' : 'dark';
      document.documentElement.dataset.theme = next;
      setThemeCookie(next);
    });

    (function () {
      const discordBtn = document.getElementById('discord-btn');
      const discordToast = document.getElementById('discord-toast');
      let toastTimer = null;

      discordBtn.addEventListener('click', async () => {
        try { await navigator.clipboard.writeText('khaddict'); } catch { /* clipboard unavailable */ }
        discordBtn.classList.add('copied');
        discordToast.classList.add('show');
        clearTimeout(toastTimer);
        toastTimer = setTimeout(() => {
          discordToast.classList.remove('show');
          discordBtn.classList.remove('copied');
        }, 900);
      });
    })();

    (function () {
      const COLS = 72, ROWS = 16;
      const MARGIN = 3; // breathing room from both edges of the screen when the text is static
      const canvas = document.getElementById('wall-screen');
      const ctx = canvas.getContext('2d');
      const textarea = document.getElementById('wall-message');
      const counter = document.getElementById('wall-counter');
      const sendBtn = document.getElementById('wall-send');
      const toast = document.getElementById('wall-toast');

      // hand-transcribed bitmap font, matches the real BUSY app pixel grid
      const FONT_H = 9;
      const Z = (w) => '0'.repeat(w);
      const FONT = {
        ' ': { w: 3, rows: [Z(3), Z(3), Z(3), Z(3), Z(3), Z(3), Z(3), Z(3), Z(3)] },
        'a': { w: 4, rows: ['0000', '0000', '0110', '0001', '0111', '1001', '0111', Z(4), Z(4)] },
        'b': { w: 4, rows: ['1000', '1000', '1110', '1001', '1001', '1001', '1110', Z(4), Z(4)] },
        'c': { w: 4, rows: ['0000', '0000', '0111', '1000', '1000', '1000', '0111', Z(4), Z(4)] },
        'd': { w: 4, rows: ['0001', '0001', '0111', '1001', '1001', '1001', '0111', Z(4), Z(4)] },
        'e': { w: 4, rows: ['0000', '0000', '0110', '1001', '1111', '1000', '0110', Z(4), Z(4)] },
        'f': { w: 4, rows: ['0010', '0101', '0100', '1111', '0100', '0100', '0100', Z(4), Z(4)] },
        'g': { w: 4, rows: ['0000', '0000', '0111', '1001', '1001', '1001', '0111', '0001', '0110'] },
        'h': { w: 4, rows: ['1000', '1000', '1110', '1001', '1001', '1001', '1001', Z(4), Z(4)] },
        'i': { w: 3, rows: ['010', '000', '110', '010', '010', '010', '111', Z(3), Z(3)] },
        'j': { w: 3, rows: ['001', '000', '111', '001', '001', '001', '001', '001', '110'] },
        'k': { w: 4, rows: ['1000', '1000', '1001', '1010', '1100', '1010', '1001', Z(4), Z(4)] },
        'l': { w: 3, rows: ['110', '010', '010', '010', '010', '010', '011', Z(3), Z(3)] },
        'm': { w: 5, rows: ['00000', '00000', '11110', '10101', '10101', '10101', '10101', Z(5), Z(5)] },
        'n': { w: 4, rows: ['0000', '0000', '1110', '1001', '1001', '1001', '1001', Z(4), Z(4)] },
        'o': { w: 4, rows: ['0000', '0000', '0110', '1001', '1001', '1001', '0110', Z(4), Z(4)] },
        'p': { w: 4, rows: ['0000', '0000', '1110', '1001', '1001', '1001', '1110', '1000', '1000'] },
        'q': { w: 4, rows: ['0000', '0000', '0111', '1001', '1001', '1001', '0111', '0001', '0001'] },
        'r': { w: 3, rows: ['000', '000', '101', '110', '100', '100', '100', '000', '000'] },
        's': { w: 4, rows: ['0000', '0000', '0111', '1000', '0110', '0001', '1110', '0000', '0000'] },
        't': { w: 4, rows: ['0100', '0100', '1111', '0100', '0100', '0100', '0011', '0000', '0000'] },
        'u': { w: 4, rows: ['0000', '0000', '1001', '1001', '1001', '1001', '0111', '0000', '0000'] },
        'v': { w: 5, rows: ['00000', '00000', '10001', '10001', '01010', '01010', '00100', '00000', '00000'] },
        'w': { w: 5, rows: ['00000', '00000', '10001', '10101', '10101', '01010', '01010', '00000', '00000'] },
        'x': { w: 5, rows: ['00000', '00000', '10001', '01010', '00100', '01010', '10001', '00000', '00000'] },
        'y': { w: 4, rows: ['0000', '0000', '1001', '1001', '1001', '1001', '0111', '0001', '0110'] },
        'z': { w: 4, rows: ['0000', '0000', '1111', '0010', '0100', '1000', '1111', '0000', '0000'] },
        '0': { w: 5, rows: ['01110', '10001', '10011', '10101', '11001', '10001', '01110', Z(5), Z(5)] },
        '1': { w: 3, rows: ['010', '110', '010', '010', '010', '010', '111', Z(3), Z(3)] },
        '2': { w: 5, rows: ['01110', '10001', '00001', '00010', '00100', '01000', '11111', Z(5), Z(5)] },
        '3': { w: 5, rows: ['11111', '00010', '00100', '00010', '00001', '10001', '01110', Z(5), Z(5)] },
        '4': { w: 5, rows: ['00010', '00110', '01010', '10010', '11111', '00010', '00010', Z(5), Z(5)] },
        '5': { w: 5, rows: ['11111', '10000', '11110', '00001', '00001', '10001', '01110', Z(5), Z(5)] },
        '6': { w: 5, rows: ['00110', '01000', '10000', '11110', '10001', '10001', '01110', Z(5), Z(5)] },
        '7': { w: 5, rows: ['11111', '00001', '00010', '00100', '01000', '01000', '01000', Z(5), Z(5)] },
        '8': { w: 5, rows: ['01110', '10001', '10001', '01110', '10001', '10001', '01110', Z(5), Z(5)] },
        '9': { w: 5, rows: ['01110', '10001', '10001', '01111', '00001', '00010', '01100', Z(5), Z(5)] },
        '?': { w: 5, rows: ['01110', '10001', '00001', '00010', '00100', Z(5), '00100', Z(5), Z(5)] },
        '!': { w: 2, rows: ['10', '10', '10', '10', '10', Z(2), '10', Z(2), Z(2)] },
        '-': { w: 4, rows: [Z(4), Z(4), Z(4), Z(4), '1111', Z(4), Z(4), Z(4), Z(4)] },
        "'": { w: 2, rows: ['10', '10', Z(2), Z(2), Z(2), Z(2), Z(2), Z(2), Z(2)] },
        '.': { w: 2, rows: [Z(2), Z(2), Z(2), Z(2), Z(2), Z(2), '10', Z(2), Z(2)] },
        ',': { w: 2, rows: [Z(2), Z(2), Z(2), Z(2), Z(2), '10', '01', Z(2), Z(2)] },
      };

      function glyphFor(ch) {
        return FONT[ch.toLowerCase()] || FONT[' '];
      }

      function textToStrip(text) {
        const glyphs = Array.from(text || ' ').map(glyphFor);
        const textW = Math.max(glyphs.reduce((sum, g) => sum + g.w + 1, 0) - 1, 1);
        // gap before the text loops when scrolling, the real bar uses 2 spaces here
        const LOOP_GAP = (FONT[' '].w + 1) * 1;
        const fits = textW <= COLS - MARGIN * 2;
        const stripW = fits ? COLS : textW + LOOP_GAP;
        const buf = new Uint8Array(stripW * FONT_H);
        let cx = fits ? MARGIN : 0;
        let minRow = FONT_H, maxRow = -1;
        glyphs.forEach((g) => {
          for (let y = 0; y < FONT_H; y++) {
            const row = g.rows[y];
            for (let x = 0; x < g.w; x++) {
              if (row[x] === '1') {
                buf[y * stripW + cx + x] = 1;
                if (y < minRow) minRow = y;
                if (y > maxRow) maxRow = y;
              }
            }
          }
          cx += g.w + 1;
        });
        // center on the actual ink used by this text, not the full glyph box: digits
        // (rows 0-6), x-height letters (2-6) and descenders (2-8) each need a different
        // vertical offset to look centered, a single fixed TEXT_Y can't fit all of them
        if (maxRow < 0) { minRow = 2; maxRow = 6; }
        const inkHeight = maxRow - minRow + 1;
        const topMargin = Math.ceil((ROWS - inkHeight) / 2);
        const textY = topMargin - minRow;
        return { buf, stripW, textW, textY };
      }

      function sampleTextWindow(strip, offsetX) {
        const out = new Float32Array(COLS * ROWS);
        const ox = Math.floor(offsetX);
        for (let y = 0; y < FONT_H; y++) {
          const screenY = y + strip.textY;
          if (screenY < 0 || screenY >= ROWS) continue;
          for (let x = 0; x < COLS; x++) {
            const srcX = (ox + x) % strip.stripW;
            out[screenY * COLS + x] = strip.buf[y * strip.stripW + srcX];
          }
        }
        return out;
      }

      let stripData = null, stripW = 0, needsScroll = false, offset = 0;
      let selectedColor = '#FFFFFF';

      function colorPillBackground(hex) {
        const r = parseInt(hex.slice(1, 3), 16);
        const g = parseInt(hex.slice(3, 5), 16);
        const b = parseInt(hex.slice(5, 7), 16);
        return `rgba(${r}, ${g}, ${b}, .3)`;
      }

      function drawBuffer(buf) {
        // real LED pixels are hard on/off, no anti-aliasing
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        const cell = canvas.width / COLS;
        ctx.fillStyle = selectedColor;
        const gap = Math.max(1, cell * 0.12);
        for (let y = 0; y < ROWS; y++) {
          for (let x = 0; x < COLS; x++) {
            if (buf[y * COLS + x] < 0.5) continue;
            ctx.fillRect(x * cell + gap, y * cell + gap, cell - gap * 2, cell - gap * 2);
          }
        }
      }

      function setText(text) {
        const strip = textToStrip(text || ' ');
        stripData = strip; stripW = strip.stripW; needsScroll = strip.textW > COLS - MARGIN * 2; offset = 0;
        if (!needsScroll) drawBuffer(sampleTextWindow(strip, 0));
      }

      function tick() {
        if (needsScroll && stripData) {
          offset = (offset + 0.15) % stripData.stripW;
          drawBuffer(sampleTextWindow(stripData, offset));
        }
        requestAnimationFrame(tick);
      }

      function updateCounter() {
        const len = textarea.value.length;
        counter.textContent = `${len}/60`;
        counter.classList.toggle('warn', len > 50);
      }

      textarea.addEventListener('input', () => { updateCounter(); setText(textarea.value); });

      let mode = 'text';
      let imageFile = null;
      let loadedImage = null;
      const textField = document.getElementById('wall-text-field');
      const imageField = document.getElementById('wall-image-field');
      const dropZone = document.getElementById('wall-drop-zone');
      const fileInput = document.getElementById('wall-file-input');

      function imageToBuffer(img) {
        const tmp = document.createElement('canvas');
        tmp.width = COLS; tmp.height = ROWS;
        const tctx = tmp.getContext('2d');
        const scale = Math.min(COLS / img.width, ROWS / img.height);
        const dw = img.width * scale, dh = img.height * scale;
        tctx.drawImage(img, (COLS - dw) / 2, (ROWS - dh) / 2, dw, dh);
        return tctx.getImageData(0, 0, COLS, ROWS).data; // RGBA, the bar's screen is color
      }

      function drawImageBuffer(data) {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        const cell = canvas.width / COLS;
        for (let y = 0; y < ROWS; y++) {
          for (let x = 0; x < COLS; x++) {
            const i = (y * COLS + x) * 4;
            const a = data[i + 3] / 255;
            if (a < 0.05) continue;
            ctx.globalAlpha = a;
            ctx.fillStyle = `rgb(${data[i]}, ${data[i + 1]}, ${data[i + 2]})`;
            ctx.beginPath();
            ctx.arc(x * cell + cell / 2, y * cell + cell / 2, cell * 0.32, 0, Math.PI * 2);
            ctx.fill();
          }
        }
        ctx.globalAlpha = 1;
      }

      const colorPicker = document.getElementById('wall-color-picker');
      const colorTrigger = document.getElementById('wall-color-trigger');
      const colorPopover = document.getElementById('wall-color-popover');

      document.querySelectorAll('.wall-mode-toggle button').forEach((btn) => {
        btn.addEventListener('click', () => {
          document.querySelectorAll('.wall-mode-toggle button').forEach((b) => b.classList.remove('active'));
          btn.classList.add('active');
          mode = btn.dataset.mode;
          textField.style.display = mode === 'text' ? '' : 'none';
          imageField.style.display = mode === 'image' ? '' : 'none';
          colorPicker.style.display = mode === 'text' ? '' : 'none';
          needsScroll = false;
          if (mode === 'text') {
            setText(textarea.value);
          } else if (loadedImage) {
            drawImageBuffer(imageToBuffer(loadedImage));
          } else {
            drawImageBuffer(new Uint8ClampedArray(COLS * ROWS * 4));
          }
        });
      });

      colorTrigger.addEventListener('click', (e) => {
        e.stopPropagation();
        const isOpen = colorPopover.classList.toggle('open');
        colorTrigger.setAttribute('aria-expanded', String(isOpen));
      });

      document.querySelectorAll('.wall-color-swatch').forEach((sw) => {
        sw.addEventListener('click', () => {
          document.querySelectorAll('.wall-color-swatch').forEach((s) => s.classList.remove('active'));
          sw.classList.add('active');
          selectedColor = sw.dataset.color;
          colorTrigger.style.background = colorPillBackground(selectedColor);
          colorPopover.classList.remove('open');
          colorTrigger.setAttribute('aria-expanded', 'false');
          if (mode === 'text' && stripData) drawBuffer(sampleTextWindow(stripData, offset));
        });
      });

      document.addEventListener('click', (e) => {
        if (colorPopover.classList.contains('open') && !colorPopover.contains(e.target) && e.target !== colorTrigger) {
          colorPopover.classList.remove('open');
          colorTrigger.setAttribute('aria-expanded', 'false');
        }
      });

      let imgErrorTimer = null;

      function loadImageFile(file) {
        if (!file || !file.type.startsWith('image/')) return;
        imageFile = file;
        loadedImage = null;
        const img = new Image();
        img.onload = () => {
          loadedImage = img;
          drawImageBuffer(imageToBuffer(img));
        };
        img.onerror = () => {
          imageFile = null;
          loadedImage = null;
          toast.classList.add('error');
          toast.textContent = t('wall.error');
          toast.classList.add('show');
          clearTimeout(imgErrorTimer);
          imgErrorTimer = setTimeout(() => toast.classList.remove('show', 'error'), 3000);
        };
        img.src = URL.createObjectURL(file);
      }

      dropZone.addEventListener('click', () => fileInput.click());
      dropZone.addEventListener('keydown', (e) => { if (e.key === 'Enter' || e.key === ' ') fileInput.click(); });
      fileInput.addEventListener('change', () => loadImageFile(fileInput.files[0]));

      ['dragenter', 'dragover'].forEach((evt) => {
        dropZone.addEventListener(evt, (e) => {
          e.preventDefault();
          dropZone.classList.add('dragover');
        });
      });
      ['dragleave', 'dragend'].forEach((evt) => {
        dropZone.addEventListener(evt, () => dropZone.classList.remove('dragover'));
      });
      dropZone.addEventListener('drop', (e) => {
        e.preventDefault();
        dropZone.classList.remove('dragover');
        loadImageFile(e.dataTransfer.files[0]);
      });

      const API_URL = '{{ api_base_url }}';

      const livePill = document.getElementById('wall-live-pill');
      const liveDot = document.getElementById('wall-live-dot');
      const liveText = document.getElementById('wall-live-text');

      let barOnline = true;

      function updateSendAvailability() {
        sendBtn.disabled = cooling || !barOnline;
      }

      function setBarOnline(online) {
        barOnline = online;
        livePill.classList.toggle('down', !online);
        liveDot.classList.toggle('up', online);
        liveDot.classList.toggle('down', !online);
        liveText.textContent = t(online ? 'wall.live' : 'wall.offline');
        updateSendAvailability();
      }

      async function refreshBarStatus() {
        try {
          const res = await fetch(`${API_URL}/busybar/status`);
          const data = await res.json();
          setBarOnline(!!data.online);
        } catch {
          setBarOnline(false);
        }
      }

      function reportResult(res) {
        if (res.ok) {
          toast.textContent = t('wall.sent');
          return 30;
        }
        toast.textContent = res.status === 429 ? t('wall.rateLimited') : t('wall.error');
        toast.classList.add('error');
        if (res.status === 429) {
          const retryAfter = parseInt(res.headers.get('Retry-After'), 10);
          if (Number.isFinite(retryAfter) && retryAfter > 0) return retryAfter;
        }
        return 30;
      }

      const sendLabel = sendBtn.textContent;
      const COOLDOWN_KEY = 'wallCooldownUntil';

      function persistCooldown(waitSecs) {
        try { localStorage.setItem(COOLDOWN_KEY, String(Date.now() + waitSecs * 1000)); } catch { /* storage unavailable */ }
      }

      function startCooldown(initialSecs) {
        let secs = initialSecs || 30;
        cooling = true;
        sendBtn.disabled = true;
        sendBtn.textContent = `${secs}s`;
        const iv = setInterval(() => {
          secs -= 1;
          if (secs <= 0) {
            clearInterval(iv);
            cooling = false;
            updateSendAvailability();
            sendBtn.textContent = sendLabel;
            toast.classList.remove('show');
            try { localStorage.removeItem(COOLDOWN_KEY); } catch { /* storage unavailable */ }
          } else {
            sendBtn.textContent = `${secs}s`;
          }
        }, 1000);
      }

      let cooling = false;

      // persist the cooldown so a refresh mid-wait doesn't show a clickable Send again
      (function restoreCooldown() {
        let until = 0;
        try { until = parseInt(localStorage.getItem(COOLDOWN_KEY), 10) || 0; } catch { /* storage unavailable */ }
        const remaining = Math.ceil((until - Date.now()) / 1000);
        if (remaining > 0) startCooldown(remaining);
      })();
      sendBtn.addEventListener('click', async () => {
        if (cooling || !barOnline) return;
        if (mode === 'text') {
          const message = textarea.value.trim();
          if (!message) return;
          cooling = true;
          sendBtn.disabled = true;
          toast.classList.remove('error');
          toast.textContent = t('wall.sending');
          toast.classList.add('show');
          let waitSecs = 30;
          try {
            const res = await fetch(`${API_URL}/wall/message`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ message, color: selectedColor }),
            });
            waitSecs = reportResult(res);
          } catch {
            toast.textContent = t('wall.error');
            toast.classList.add('error');
          }
          persistCooldown(waitSecs);
          startCooldown(waitSecs);
        } else {
          if (!loadedImage) return;
          cooling = true;
          sendBtn.disabled = true;
          toast.classList.remove('error');
          toast.textContent = t('wall.sending');
          toast.classList.add('show');
          let waitSecs = 30;
          try {
            const form = new FormData();
            form.append('file', imageFile);
            const res = await fetch(`${API_URL}/wall/image`, { method: 'POST', body: form });
            waitSecs = reportResult(res);
          } catch {
            toast.textContent = t('wall.error');
            toast.classList.add('error');
          }
          persistCooldown(waitSecs);
          startCooldown(waitSecs);
        }
      });

      updateCounter();
      setText(textarea.value);
      tick();
      let statusInterval = null;

      function startStatusPolling() {
        refreshBarStatus();
        if (!statusInterval) statusInterval = setInterval(refreshBarStatus, 30000);
      }

      document.addEventListener('visibilitychange', () => {
        if (document.hidden) {
          clearInterval(statusInterval);
          statusInterval = null;
        } else {
          startStatusPolling();
        }
      });

      startStatusPolling();
    })();
  </script>
</body>
</html>
